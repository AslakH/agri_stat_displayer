# Play-Agricola HTML Snapshots

If direct URL fetching fails, place Play-Agricola card table HTML files in this folder and run:

```bash
npm run data:ingest:playagricola
```

The ingester parses the largest valid table file in this folder.
